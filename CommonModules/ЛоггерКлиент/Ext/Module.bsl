
Процедура ПоказатьСообщение(Период, Уровень, Объект, Текст) Экспорт
	
	ОткрытьФорму("ОбщаяФорма.Сообщение",
		Новый Структура("Сообщение", 
			Новый Структура("Период, Уровень, Объект, Текст",
			    Период,
				Уровень,
				Объект,
				Текст)),
		,
		,
		,
		,
		,
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры